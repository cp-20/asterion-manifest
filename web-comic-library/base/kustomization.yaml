apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployments.yaml
  - services.yaml
  - jobs.yaml

generators:
  - ksops.yaml

generatorOptions:
  disableNameSuffixHash: true
